# AGENTS.md

## リポジトリ概要

このリポジトリは、毎年の POG（ペーパーオーナーゲーム）の候補馬をリストアップするための静的サイトです。

年ごとに候補馬の記事ページを分け、各年の `yyyy/index.html` にその年の候補馬を掲載します。たとえば 2026 年の候補馬は `2026/index.html` にまとめます。

## コンテンツ方針

- 各年のページには、その年の POG 候補馬を一覧できるように掲載する。
- 各馬の記事には、馬の写真と基本情報を載せる。
- 具体的にどんな情報を載せるかは、作業ごとにユーザーへ確認する。
- 写真の出典、権利、表示可否が不明な場合は、勝手に追加せず確認する。
- 長く運用するページなので、後から読み返しても分かりやすい HTML 構造と簡潔な文面を心がける。

## ディレクトリ方針

- ルートの `index.html` は、年別ページへの入口として扱う。
- 年別ページは `yyyy/index.html` の形式で作成する。
- 年別の画像や補助ファイルが必要な場合は、原則としてその年のディレクトリ配下に置く。

例:

```text
.
├── index.html
├── 2026/
│   ├── index.html
│   └── images/
│       └── horse-name.jpg
└── 2027/
    └── index.html
```

## 技術方針

- 長期運用しやすくするため、外部ライブラリやビルドツールは導入しない。
- HTML、CSS、JavaScript だけで実装する。
- フレームワーク、パッケージマネージャ、バンドラー、CSS ライブラリは追加しない。
- ブラウザでそのまま開ける静的なページとして維持する。
- CSS や JavaScript を分ける場合も、必要最小限にとどめる。
- PC とスマートフォンの両方で閲覧しやすいレスポンシブデザインにする。
- 横幅が狭い画面でも文字、画像、リンク、カードがはみ出さないようにする。

## 作業時の注意

- 既存の年別ページがある場合は、その構造や見た目に合わせて編集する。
- 新しい年のページを作る場合は、`yyyy/index.html` を作成する。
- 馬の情報項目、表示順、文体に迷う場合は、実装前にユーザーへ確認する。
- 不確かな血統、戦績、馬主、生産者、厩舎などの情報は、推測で書かない。

## このページの作り方（来年の自分用）

1. NotebookLMで参考文献集め
2. NotebookLM上で50頭に絞り込む
```
POGおすすめの馬を上位50頭 おすすめ順で表示して。
表示順は 馬名 | 母 | 父 で表示して
```
3. NotebookLMでMDファイルを作成&`./YYYY`ディレクトリにコピー
```
50頭すべてに、馬名・母・父・参考ページが入っている。
各馬に少なくとも1つ以上の根拠リンクがある。
画像は保存できた馬だけMarkdownに相対パスで掲載し、保存できなかった馬は「画像未取得」と理由を書く。
不確かな血統、厩舎、馬主、生産者、入厩情報は断定しない。
Markdownのリンク切れ、画像パス、見出し階層を最後に確認する。

各馬の構成は固定する。
順位、馬名、母、父
画像
基本情報: 性別、生年月日、毛色、厩舎、生産者、馬主、馬名由来など判明分
父の特色
母の特色、母自身の実績や近親
POG的な楽しみどころ: 人気、早期始動、厩舎/馬主/牧場、血統の見どころ、専門家・媒体での推奨材料
参考ページ
未確認メモ
Source Policy


基本データと指名人気の軸は POG-INFO YYYY年度 指名馬ランキング と各馬ページを使う。
シーズン全体の文脈は netkeiba POGYYYY-YYYY 有力馬まとめ を参照する。
期待値、推奨コメント、入厩/ゲート試験などの読み物要素は POG-UP 系記事や、検索で見つかる競馬メディア/クラブ/牧場/ニュースを補助ソースとして使う。
父・母の特色は、推測せず、POG-INFO、netkeiba、JBIS、JRA、クラブ公式、牧場/セール情報など確認できるページを優先する。
情報が割れる場合は断定せず、「Aではこう、Bでは未確認」の形で残す。

読み物として楽し情報も収集対象。
```

4. AIで生成
```
今年 YYYY年のディレクトリのmdを参照して、index.htmlを書いて
各馬のリンクにnetkeibaのリンクが欲しい。
画像はなしでOK。
TOPに各馬へジャンプできるような機能にして。
ページのデザインは毎年変わってもOK。読んでて楽しい感じのページにして。
PC/SP両方対応で
TOPページからもリンクで飛べるようにして
```